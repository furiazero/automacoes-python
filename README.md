# Contador automático de palavras, Caracteres e linhas.

Este mini-projeto é um script que abre um arquivo.txt com a função open() nativa do python
usando o modo "w" ele escreve um texto com a função read() ele faz a leitura do texto.

Com isso ele consegue o resultado da :

- Quantidadde de **linhas***
- Quantidade de **palavras***
- Quantidade de **caracteres***

É um exercício simples para praticar manipulação de arquivos e strings em Python.

---

## 🚀 Como rodar o script

1. Coloque um arquivo chamado `entrada.txt` na mesma pasta do script.
2. Execute o arquivo Python:

```bash
python contador.py
